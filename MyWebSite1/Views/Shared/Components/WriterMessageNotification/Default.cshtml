
@*No:92 Yazar Navbar üst menü düzenlemesi için WriterLayouttan alındı
*@

@*No:104 Mesajlar için model yazıldı,foreach ile mesajlar getirildi ve mesaj ikonunda mesaj göndericisi ve tarih ayarlandı
*@

@using EntityLayer.Concrete
@model List<Message2>


<div class="dropdown-menu dropdown-menu-right navbar-dropdown preview-list" aria-labelledby="messageDropdown">
    <h6 class="p-3 mb-0">Mesajlar</h6>
    <div class="dropdown-divider"></div>

    @foreach(var item in Model){
    <a class="dropdown-item preview-item">
        <div class="preview-thumbnail">

    @*            No:108 Yazar resimleri getirmek için yazıldı
    *@            <img src="@item.SenderUser.WriterImage" alt="image" class="profile-pic">
        </div>
        <div class="preview-item-content d-flex align-items-start flex-column justify-content-center">

               @* nO:108 @item.SenderID->@item.ReceiverUser.WriterName olarak değiştirildi*@
            <h6 class="preview-subject ellipsis mb-1 font-weight-normal">@item.SenderUser.WriterName sana mesaj gönderdi!</h6>
            <p class="text-gray mb-0"> @item.MessageDate.ToShortDateString() </p>
        </div>
    </a>
    }
   
    <h6 class="p-3 mb-0 text-center">3 Yeni Mesaj <h6>
</div>
